package spaceExplorer.FoodItems;

import spaceExplorer.Consumable;

/** Represents a Burger item, which extends the Consumable abstract class.
 * @author Karna Malone
 * @author Daniel Watson
*/
public class Burger extends Consumable{
	
	/** 
	 * Initializes Burger with predefined values.
	*/
	public Burger() {
		super("Burger", 25, 0, 10);
	 }
}
